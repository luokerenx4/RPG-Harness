name: hub:upgrade_mundane 資源不足時は public action を lock（D 分割後）
description: |
  Pulse-system update — upgrade_weapon is split into upgrade_mundane/
  upgrade_pure/upgrade_oni. With no shards, the same public snapshot used by
  GUI and Headless clients locks the mundane path and explains the missing
  resource instead of advertising an input that can only narrate denial.
state:
  baseline:
    completedScripts:
      - 000_intro
    weapons:
      ancestor_yaodao:
        power: 4
  sengoku-raid:
    raid: null
    metCharacters: []
inputs: []
assertions:
  - kind: state
    path: baseline.weapons.ancestor_yaodao.power
    eq: 4
  - kind: activity
    id: upgrade_mundane
    available: false
    lockedReasonIncludes: 魂石の欠片
